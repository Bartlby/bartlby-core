#!/bin/sh
BARTLBY_HOME=@prefix@
useradd bartlby 2>&1>>/dev/null
if [ -f ${BARTLBY_HOME}/etc/bartlby.cfg ];
then
	echo "backing up bartlby.cfg";
	cp ${BARTLBY_HOME}/etc/bartlby.cfg ${BARTLBY_HOME}/etc/bartlby.cfg.UPGRADE
fi;
export BARTLBY_UPGRADE="true";
exit 0;
